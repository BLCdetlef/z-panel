# Installation

Diese vier Dateien in das Hauptverzeichnis von `z-panel` hochladen:

- index.html
- news.css
- news.js
- news.json

Die vorhandene `index.html` wird ersetzt. Der Ordner `generator/` bleibt unverändert.

Commit-Nachricht:

News-Panel Version 1 eingerichtet
